PORT=3000
# Static token protecting mutating routes, push subscription routes, and /api/stream.
# Generate with: openssl rand -hex 24 (leave empty to disable the gate in local dev)
API_TOKEN=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=mailto:your@email.com
FRONTEND_URL=http://localhost:5173
